{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run current file (conda base)",
      "type": "shell",
      "command": "/opt/anaconda3/bin/python",
      "args": ["${file}"],
      "options": { "cwd": "${workspaceFolder}" },
      "group": { "kind": "build", "isDefault": true },
      "presentation": { "reveal": "always", "panel": "shared", "clear": true },
      "problemMatcher": []
    }
  ]
}
